ATTENZIONE AL NOME DEL DB IN DriverManagerConnectionPool
CREA CARTELLA db, control, model
AGGIUNGI dependency in pom.xml:
    <dependency>
        <groupId>mysql</groupId>
        <artifactId>mysql-connector-java</artifactId>
        <version>8.0.34</version> <!-- Check for the latest version -->
    </dependency>
CREA CARTELLA lib IN WEB-INF E AGGIUNGI IL jar